[general]
primary_client = "coconut"
spawn_primary_client = true
backend = "auto"
# Watches existing configuration files and directories after startup.
hot_reload = true

[integrations]
# Enables the built-in D-Bus transport integration.
dbus = true

[input.keyboard]
layout = "us"
variant = ""
repeat_delay = 250
repeat_rate = 35

[input.mouse]
sensitivity = 0.0
acceleration = "adaptive"

[input.touchpad]
tap = true
natural_scroll = true
disable_while_typing = true

[[bindings]]
keys = ["SUPER", "Q"]
action = "close"

[[bindings]]
keys = ["SUPER", "RETURN"]
exec = "foot"

[[bindings]]
keys = ["SUPER", "1"]
action = "workspace"
value = 1

[[bindings]]
keys = ["SUPER", "SHIFT", "1"]
action = "move-to-workspace"
value = 1

# Omit this table for the safe automatic profile: detected outputs remain
# enabled using their preferred mode. Names match DRM connector names.
[outputs."DP-1"]
enabled = true
mode = "2560x1440@165"
position = [0, 0]
scale = 1.0
transform = "normal"
vrr = true

[outputs."HDMI-A-1"]
position = [2560, 0]
scale = 1.0

[window]
default_width = 900
default_height = 600
server_side_decorations = true
layout = "floating"
work_area_padding = 16

[decoration]
titlebar_height = 32
border_width = 4
corner_radius = 12
active_titlebar = "#1e1e2e"
inactive_titlebar = "#11111b"
active_border = "#89b4fa"
inactive_border = "#313244"
close_button = "#f38ba8"
maximize_button = "#a6e3a1"
minimize_button = "#f9e2af"
